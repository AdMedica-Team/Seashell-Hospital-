const YT_ID = "F6OfQzOhiz4";

// Single hero video, shared by every style demo — embedded via youtube-nocookie,
// muted/looping/autoplay so it behaves like the local clips it replaces.
export const YT_EMBED_SRC =
  `https://www.youtube-nocookie.com/embed/${YT_ID}` +
  `?autoplay=1&mute=1&loop=1&playlist=${YT_ID}` +
  `&controls=0&modestbranding=1&rel=0&playsinline=1&disablekb=1&iv_load_policy=3`;
